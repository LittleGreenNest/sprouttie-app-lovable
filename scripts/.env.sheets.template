# Sprouttie Sheet Sync — environment config
# Copy this file to scripts/.env.sheets and fill in the values below.
# .env.sheets is gitignored — never commit it.

# Stripe live secret key (sk_live_...)
# Get from: stripe.com/dashboard → Developers → API Keys
STRIPE_SECRET_KEY=

# Supabase service role key (NOT the anon key)
# Get from: supabase.com → Project Settings → API → service_role
SUPABASE_SERVICE_ROLE_KEY=

# Optional: override the default Google service account JSON path
# GOOGLE_CREDS_PATH=/Users/cyrenachio/Downloads/opensofthr-website-revamp-adcbbda13610.json
